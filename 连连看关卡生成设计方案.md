# 连连看关卡生成设计方案

> 本文档基于当前连连看游戏的实际机制设计：
> - 网格 8×10（80 格，40 对）
> - 图标类型最多 8 种
> - 路径规则：最多 2 个拐角（0/1/2 次转弯）
> - 路径可经网格边界外虚拟区域通行
> - 起始状态：网格全满（无空格）
> - 无有效移动时自动重排

---

## 一、设计目标

1. **可解性保证**：生成的关卡必须 100% 可通关
2. **难度可量化**：用明确的数值指标评价关卡难度，而非主观感受
3. **难度可调控**：通过参数控制生成不同难度等级的关卡
4. **指标可解释**：每个指标的选择都有认知科学或博弈论依据

---

## 二、难度维度分析

连连看的难度不是单一因素，而是三个维度的综合：

```
┌─────────────────────────────────────────────────────┐
│                  总难度 (DD)                         │
│                                                     │
│  ┌─────────────┐ ┌─────────────┐ ┌──────────────┐  │
│  │ 视觉感知难度 │ │ 路径推理难度 │ │ 策略规划难度  │  │
│  │   (VPD)     │ │   (PRD)     │ │   (SPD)     │  │
│  │             │ │             │ │              │  │
│  │ 找到配对    │ │ 验证连通    │ │ 选择消除顺序  │  │
│  └─────────────┘ └─────────────┘ └──────────────┘  │
└─────────────────────────────────────────────────────┘
```

| 维度 | 玩家面临的挑战 | 影响因素 |
|------|---------------|---------|
| **视觉感知难度 VPD** | 在密集图标中找到两个相同的 | 网格大小、类型数、同类型空间离散度 |
| **路径推理难度 PRD** | 判断两个图标之间能否连通 | 路径转弯数、路径长度、空格布局 |
| **策略规划难度 SPD** | 决定先消哪对、后消哪对 | 有效解数量、死锁风险、决策宽度 |

---

## 三、量化指标体系

共定义 **8 个指标**，分为三组对应三个难度维度。

---

### 第一组：视觉感知难度（VPD）指标

#### 指标 M1：初始有效解密度 VMD（Valid Move Density）

**定义**：游戏开始时（网格全满），可连通配对数与理论最大配对数的比值。

**公式**：
```
VMD = IVPC / TPP

其中：
  IVPC = Initial Valid Pair Count（初始可连通配对数）
  TPP  = Total Possible Pairs（理论配对总数）
       = Σ C(n_i, 2)   对每个类型 i，n_i 为该类型的瓦片数
       C(n,2) = n×(n-1)/2
```

**取值范围**：`[0, 1]`

**测量方法**：遍历所有同类型瓦片的两两组合，调用 `PathFinder.FindPath()` 检测连通性，统计可连通的对数。

**合理性论证**：

> **关键洞察**：在网格全满时，所有内部格子都被占据，路径只能通过边界外的虚拟区域通行。因此初始阶段只有**边缘瓦片**才可能被连通，且路径必须绕到网格外部。这使得 IVPC 通常远小于 TPP。

- VMD 越低，玩家在满屏图标中能立即消除的对越少，搜索成本越高
- 这对应认知科学中的"信号检测论"：有效信号（可消对）在噪声（不可消对）中的比例越低，检测越困难
- 极端情况 VMD≈0 意味着开局几乎无解，玩家被迫盲目点击，挫败感极强

---

#### 指标 M2：图标类型熵 TTE（Tile Type Entropy）

**定义**：图标类型分布的香农熵，衡量图标种类的多样性和均匀度。

**公式**：
```
TTE = -Σ p_i × log2(p_i)

其中：
  p_i = n_i / N    （类型 i 的出现频率）
  n_i = 类型 i 的瓦片数
  N   = 总瓦片数（= 80）
```

**取值范围**：`[0, log2(K)]`，K 为类型总数。当所有类型均匀分布时取最大值。

**测量方法**：统计每种类型的瓦片数，计算频率分布的香农熵。

**合理性论证**：

- 类型越多且分布越均匀，玩家需要在更多视觉类别中区分和匹配，认知负荷越高
- 香农熵同时度量了"种类数"和"均匀度"两个因素：8 种均匀分布比 4 种集中分布更难
- 这符合米勒的"7±2"理论：人类工作记忆同时处理的信息块有限，类型数接近或超过 7 时显著增加搜索难度
- 当 TTE 接近最大值 `log2(8) = 3.0` 时，8 种类型均匀分布（每种 10 个），视觉搜索最困难

---

#### 指标 M3：同类型空间离散度 TSD（Type Spatial Dispersion）

**定义**：同类型瓦片之间的平均曼哈顿距离，衡量相同图标在空间上的分散程度。

**公式**：
```
TSD = (1/K) × Σ_k DSD_k

DSD_k = (2 / (n_k × (n_k - 1))) × Σ_{i<j} dist(t_i, t_j)

其中：
  K       = 类型总数
  DSD_k   = 类型 k 的类内离散度
  n_k     = 类型 k 的瓦片数
  t_i, t_j = 类型 k 的第 i、j 个瓦片
  dist    = 曼哈顿距离 |r1-r2| + |c1-c2|
```

**取值范围**：`[1, rows+cols-2]`（最小为相邻瓦片距离 1，最大为对角瓦片距离）

**测量方法**：对每种类型，计算其所有瓦片两两之间的曼哈顿距离平均值，再对所有类型取平均。

**合理性论证**：

- 相同图标距离越远，玩家需要在更大范围内扫描才能找到配对，视觉搜索时间越长
- 这对应视觉搜索任务中的"目标-干扰物距离效应"：目标距离越远，搜索反应时越长
- 当同类型瓦片聚集在一起时（TSD 低），玩家一眼就能看到配对；分散时（TSD 高）需要逐一扫描
- 曼哈顿距离（而非欧氏距离）的选择理由：连连看的路径基于网格移动，曼哈顿距离直接反映路径的网格步数，比欧氏距离更贴合游戏机制

---

### 第二组：路径推理难度（PRD）指标

#### 指标 M4：平均路径转弯数 APT（Average Path Turns）

**定义**：所有当前可连通配对的最短路径转弯数的平均值。

**公式**：
```
APT = (1/VPC) × Σ turns(p)

其中：
  VPC    = 当前可连通配对数（Valid Pair Count）
  turns(p) = 配对 p 的路径转弯数 = path.Count - 2
            （路径点数减去起点和终点 = 拐角数，取值 0/1/2）
```

**取值范围**：`[0, 2]`

**测量方法**：对每个可连通配对，`PathFinder.FindPath()` 返回的路径点数减 2 即为转弯数（0=直线，1=L形，2=Z/U形），对所有可连通配对求平均。

**合理性论证**：

- 0 拐角（直线）：一目了然，验证几乎零成本
- 1 拐角（L形）：需要确认拐点处是否通畅，中等推理成本
- 2 拐角（Z/U形）：需要同时确认两个拐点和三段路径，推理成本最高
- 路径转弯数直接对应空间工作记忆的负载：每增加一个拐角，玩家需要在脑中多维护一个中间节点
- APT=2 意味着所有可消对都需要最复杂的 2 拐角路径，玩家几乎无法直觉判断，必须逐格验证

---

#### 指标 M5：复杂路径占比 CPR（Complex Path Ratio）

**定义**：可连通配对中需要 2 拐角路径的配对所占比例。

**公式**：
```
CPR = (count of pairs with turns == 2) / VPC
```

**取值范围**：`[0, 1]`

**测量方法**：统计所有可连通配对中转弯数为 2 的配对数量，除以总可连通配对数。

**合理性论证**：

- APT 是平均值，可能掩盖极端分布：如果一半是 0 拐角、一半是 2 拐角，APT=1 看似中等，但实际难度两极分化
- CPR 单独衡量最困难路径的占比，补充 APT 的不足
- 2 拐角路径需要绕到网格边界外或穿过内部空格，是最难被玩家发现的连通方式
- CPR=1 表示所有可消对都需要最复杂的路径，玩家几乎找不到"轻松"的消除机会

---

### 第三组：策略规划难度（SPD）指标

#### 指标 M6：决策宽度 DW（Decision Width）

**定义**：游戏中每个时刻可选择的有效配对数量的平均值（通过模拟完整游戏过程计算）。

**公式**：
```
DW = (1/M) × Σ_{step=1}^{M} VPC(step)

其中：
  M         = 总消除步数（= 40 对）
  VPC(step) = 第 step 步消除前的可连通配对数
```

**取值范围**：`[1, TPP]`

**测量方法**：使用 Bot 的 `FindBestPair` 逻辑模拟一整局游戏（选择拐角最少+距离最近的配对），在每步消除前记录可连通配对数 VPC，最后取平均。

**合理性论证**：

- 决策宽度衡量玩家面临的选择空间：DW=1 意味着每步只有唯一选择（被迫操作），DW 高意味着选择多
- **DW 与难度的关系是非线性的**：
  - DW 过低（接近 1）：虽然不需要决策，但需要找到那个唯一解，搜索难度极高（"大海捞针"）
  - DW 过高：选择过多，玩家容易选择"错误"的消除顺序导致后续死锁（选择困难）
  - DW 适中（3-6）：有足够选择空间，但不需要深度规划，难度适中
- 这对应决策心理学中的"选择悖论"：过多选择反而增加决策负担
- 用模拟而非静态计算，因为 VPC 在游戏过程中动态变化，需要反映整个游戏过程的平均体验

---

#### 指标 M7：死锁频率 DF（Deadlock Frequency）

**定义**：通过蒙特卡洛模拟，统计在自然游戏过程中出现"无有效移动"（需要触发自动重排）的频率。

**公式**：
```
DF = (count of deadlock events) / (total simulation steps)

一次完整模拟：用 Bot 算法从头玩到通关，记录每次 HasValidMoves()=false 的时刻
```

**取值范围**：`[0, 1]`

**测量方法**：运行 N 次（建议 N≥100）完整游戏模拟，每次用随机配对选择策略（非最优策略，更接近真实玩家行为），统计死锁发生次数与总步数的比值。

**合理性论证**：

- 死锁意味着玩家无法继续，被迫等待自动重排，打断了游戏的流畅体验
- 死锁频率高意味着关卡设计"陷阱"多，玩家容易走入死胡同
- 用随机策略而非最优策略模拟，因为真实玩家不会总是选择最优配对，随机策略更能反映真实游戏体验的死锁概率
- 注意：当前游戏有自动重排机制，死锁不会导致卡死，但频繁重排会破坏玩家的空间记忆和计划，降低游戏体验

---

#### 指标 M8：关卡解序列分支度 SB（Solution Branching）

**定义**：关卡从初始状态到通关，所有合法消除序列的总数（或其数量级估计）。

**公式**：
```
SB = Π_{step=1}^{M} VPC(step)

由于直接计算可能数值溢出，实际使用对数形式：
log(SB) = Σ_{step=1}^{M} log(VPC(step))
```

**取值范围**：`[0, ∞)`（使用对数形式通常在 0~100 范围）

**测量方法**：在指标 M6 的模拟过程中，记录每步的 VPC，计算其对数累加和。

**合理性论证**：

- SB 衡量关卡的"自由度"：消除顺序有多少种合法排列
- SB 高意味着关卡高度灵活，消除顺序不影响通关，策略要求低（但搜索空间大）
- SB 低意味着消除顺序受限，某些关键步骤必须按特定顺序执行，策略要求高
- 用对数形式避免数值溢出，同时使指标在直觉上更线性
- 这对应组合博弈论中的"博弈树大小"概念，是衡量游戏复杂度的标准方法

---

## 四、综合难度评分模型

### 4.1 指标归一化

将每个指标归一化到 `[0, 1]` 区间，1 表示该维度最难：

| 指标 | 原始范围 | 归一化方式 | 归一化后含义 |
|------|---------|-----------|-------------|
| M1 VMD | [0, 1] | `1 - VMD` | VMD 越低越难 → 取反 |
| M2 TTE | [0, log2(K)] | `TTE / log2(K)` | 熵越高越难 |
| M3 TSD | [1, R+C-2] | `(TSD - 1) / (R+C-3)` | 离散度越高越难 |
| M4 APT | [0, 2] | `APT / 2` | 转弯越多越难 |
| M5 CPR | [0, 1] | `CPR`（已归一化） | 复杂路径越多越难 |
| M6 DW | [1, TPP] | 见下方说明 | 见下方说明 |
| M7 DF | [0, 1] | `DF`（已归一化） | 死锁越多越难 |
| M8 SB | [0, ∞) | `log(SB) / log(TPP)` | 见下方说明 |

**M6 DW 的特殊处理**（U型难度曲线）：

DW 与难度不是单调关系，而是 U 型曲线：
- DW < 2：选择太少，搜索困难，难度高
- DW ∈ [3, 8]：选择适中，难度低
- DW > 15：选择过多，决策困难，难度高

```
DW_norm = 
  1.0                          if DW < 2
  1 - (DW - 2) / 6            if 2 ≤ DW ≤ 8      （线性下降到 0）
  (DW - 8) / (TPP - 8)        if DW > 8           （线性上升到 1）
```

**M8 SB 的处理**：

SB 衡量自由度，过高或过低都增加难度（类似 DW）：
- SB 极低：消除顺序高度受限，策略难度高
- SB 极高：搜索空间巨大，视觉/路径推理难度高
- SB 适中：难度最低

```
SB_norm 采用与 DW 类似的 U 型归一化
```

### 4.2 维度聚合

每个维度取其下属指标的加权平均：

```
VPD = w1 × (1 - VMD) + w2 × TTE_norm + w3 × TSD_norm
PRD = w4 × APT_norm + w5 × CPR
SPD = w6 × DW_norm + w7 × DF + w8 × SB_norm
```

### 4.3 总难度评分

```
DD = α × VPD + β × PRD + γ × SPD

其中 α + β + γ = 1
```

**推荐权重**（基于认知负荷理论，路径推理是连连看的核心挑战）：

```
α = 0.30  （视觉感知）
β = 0.45  （路径推理 — 核心机制，权重最高）
γ = 0.25  （策略规划）
```

### 4.4 难度等级划分

| DD 范围 | 难度等级 | 关卡特征描述 |
|---------|---------|-------------|
| 0.00 - 0.20 | 极易 | 少类型、同类型聚集、直线路径多、选择充足 |
| 0.20 - 0.40 | 简单 | 类型适中、路径简单（多为 0-1 拐角）、死锁少 |
| 0.40 - 0.60 | 普通 | 标准配置、2 拐角路径占一定比例、偶有死锁 |
| 0.60 - 0.80 | 困难 | 多类型均匀分布、2 拐角路径为主、决策宽度低 |
| 0.80 - 1.00 | 极难 | 类型多且离散、所有路径需 2 拐角、频繁死锁、消除顺序受限 |

---

## 五、关卡生成算法设计

### 5.1 核心算法：反向构造法

**核心思想**：从空网格开始，按"消除顺序的逆序"放置配对，每放置一对时验证其在当前状态下可连通，从而保证正向游玩时一定可解。

```
反向构造 = 先放的瓦片最后被消除，后放的瓦片最先被消除
```

**算法步骤**：

```
输入：网格大小 R×C，类型数 K，目标难度 DD_target
输出：可解的瓦片布局 gridData

1. 初始化空网格（所有格子为 null）

2. 生成消除顺序序列：
   生成 totalPairs 个配对，每个配对分配一个类型
   序列顺序 = [pair_N, pair_{N-1}, ..., pair_1]
   pair_1 是第一个被消除的（最后放置）
   pair_N 是最后一个被消除的（第一个放置）

3. FOR step = N DOWNTO 1:
     a. 选择类型 typeId
     b. 在网格中找到两个空位 (r1,c1) 和 (r2,c2) 放置该类型的瓦片
     c. 验证：在当前网格状态下，(r1,c1) 到 (r2,c2) 存在可连通路径
        —— 因为 step 及之后放置的瓦片都还在，step 之前的（已消除）位置是空的
     d. 若验证通过：放置瓦片，继续下一步
        若验证失败：换一组位置重试

4. 所有配对放置完毕，得到完整的初始网格
```

**可解性证明**：

- pair_1（最后放置，最先消除）：放置时网格只有 pair_1，路径必然存在 ✓
- pair_k（第 k 个放置，第 k 个消除）：消除时，pair_1..pair_{k-1} 已被消除（位置为空），pair_k..pair_N 仍在。放置时验证了此状态下可连通 ✓
- 因此正向游玩时，每一步消除都有合法路径 ✓

### 5.2 难度调控策略

反向构造法保证可解性后，通过以下参数控制难度：

#### 调控参数表

| 参数 | 影响 | 易 | 中 | 难 |
|------|------|-----|-----|-----|
| 类型数 K | M2 TTE | 3-4 | 6 | 8 |
| 配对放置策略 | M1 VMD, M3 TSD | 同类型尽量相邻 | 随机 | 同类型尽量分散 |
| 路径约束 | M4 APT, M5 CPR | 优先选直线可达位置 | 无约束 | 优先选需 2 拐角的位置 |
| 位置选择策略 | M6 DW, M8 SB | 优先选有多解的位置 | 随机 | 优先选唯一解的位置 |

#### 各难度等级的生成策略

**极易关卡生成**：
```
- K = 3 种类型，每种 13-14 对
- 放置时优先将同类型瓦片放在相邻位置（降低 TSD）
- 放置时优先选择直线可达的空位（降低 APT）
- 选择类型时优先复用当前最少的类型（降低 TTE）
```

**普通关卡生成**：
```
- K = 6 种类型，均匀分布
- 随机选择空位放置（TSD 自然分布）
- 不对路径转弯数做约束（APT 自然分布）
- 类型随机分配
```

**极难关卡生成**：
```
- K = 8 种类型，严格均匀分布（每种 5 对）
- 放置时优先将同类型瓦片放在距离最远的位置（最大化 TSD）
- 放置时优先选择需要 2 拐角路径才能连通的空位（最大化 APT/CPR）
- 放置时优先选择使当前可连通配对数最少的位置（最小化 DW → 最大化难度）
```

### 5.3 位置选择算法（带难度倾向）

```
函数 SelectPositions(grid, typeId, difficultyPreference):
  候选位置列表 = 所有空格子
  
  FOR each (pos1, pos2) in 候选位置的两两组合:
    验证 pos1→pos2 在当前网格状态下可连通
    若可连通:
      记录 (pos1, pos2, path_turns, path_length)
  
  从所有可行组合中，按 difficultyPreference 选择:
    - "easy":   选 turns 最小、同类型距离最近的
    - "normal": 随机选一个可行的
    - "hard":   选 turns 最大、同类型距离最远的
  
  返回选中的 (pos1, pos2)
```

### 5.4 生成后验证与调整

```
1. 生成关卡布局
2. 计算所有 8 个指标
3. 计算综合难度 DD
4. IF |DD - DD_target| > 容差:
     调整参数重新生成，或局部微调瓦片位置
5. 运行可解性验证（模拟完整通关）
6. 若不可解（理论上不会发生，但作为安全检查）：
     丢弃，重新生成
```

---

## 六、指标选择的合理性总结

### 6.1 完备性论证

| 难度来源 | 对应指标 | 覆盖说明 |
|---------|---------|---------|
| 图标太多看不过来 | M1 VMD | 有效解密度低 = 大海捞针 |
| 图标种类太多分不清 | M2 TTE | 类型熵高 = 视觉分类负荷大 |
| 配对图标离太远 | M3 TSD | 空间离散度高 = 搜索范围大 |
| 路径太弯看不出来 | M4 APT | 转弯多 = 空间推理负荷大 |
| 全是复杂路径 | M5 CPR | 2 拐角占比高 = 无简单解 |
| 选择太少/太多 | M6 DW | U型曲线 = 搜索难或决策难 |
| 容易走进死胡同 | M7 DF | 死锁频率高 = 关卡设计陷阱多 |
| 消除顺序受限 | M8 SB | 分支度低 = 策略约束强 |

### 6.2 正交性论证

8 个指标应尽量正交（独立衡量不同因素），避免重复计数：

| 指标对 | 关系 | 说明 |
|--------|------|------|
| M1 VMD vs M4 APT | 独立 | VMD 衡量"有多少对可消"，APT 衡量"这些对有多难连" |
| M2 TTE vs M3 TSD | 独立 | TTE 衡量"类型多样性"，TSD 衡量"同类型空间分散度" |
| M5 CPR vs M4 APT | 部分相关 | CPR 是 APT 的补充，单独衡量最复杂路径的极端占比 |
| M6 DW vs M8 SB | 部分相关 | DW 是瞬时选择数，SB 是全局选择空间，互补使用 |
| M7 DF vs 其他 | 独立 | DF 是动态模拟结果，其他多为静态/单步指标 |

M5 和 M4、M6 和 M8 存在部分相关性，但它们分别从"极端值"和"全局值"角度补充信息，故意保留以获得更全面的难度画像。

### 6.3 可计算性论证

所有 8 个指标均可基于现有代码直接计算：

| 指标 | 依赖的现有函数 | 计算复杂度 |
|------|---------------|-----------|
| M1 VMD | `PathFinder.FindPath()` | O(K² × N²)，N 为同类型瓦片数 |
| M2 TTE | 遍历 gridData 统计 | O(N) |
| M3 TSD | 遍历 gridData 统计 | O(N²) |
| M4 APT | `PathFinder.FindPath()` 返回路径 | O(K² × N²) |
| M5 CPR | 同 M4 | 同 M4 |
| M6 DW | `FindBestPair` 模拟 + `HasValidMoves` | O(M × K² × N²)，M=步数 |
| M7 DF | 蒙特卡洛模拟 × N 次 | O(N × M × K² × N²) |
| M8 SB | M6 模拟过程中记录 | 无额外开销 |

M7 的计算成本最高（蒙特卡洛模拟），但只需在关卡生成后计算一次（非实时），性能可接受。

---

## 七、方案验证与校准

### 7.1 验证流程

```
1. 生成 5 个难度等级 × 20 个关卡 = 100 个测试关卡
2. 对每个关卡计算 8 个指标和综合难度 DD
3. 验证同一难度等级内 DD 的方差（应较小，说明等级内一致性好）
4. 验证不同难度等级间 DD 的差异（应显著，说明等级区分度好）
5. 人工试玩验证：请测试者盲测难度，与 DD 评分做相关性分析
```

### 7.2 校准方法

```
1. 收集人工难度评分（1-5 分）和 DD 评分
2. 计算两者相关系数（目标 r > 0.7）
3. 若相关性不足，调整权重 (w1-w8, α-γ)
4. 使用线性回归拟合 DD = f(人工评分)，校准参数
5. 重新验证，迭代直到满意
```

### 7.3 边界情况处理

| 情况 | 处理方式 |
|------|---------|
| 生成失败（找不到合法位置） | 降低难度约束，重新生成 |
| DD 超出 [0,1] | 截断到边界值 |
| 蒙特卡洛模拟不稳定 | 增加模拟次数 N（建议 ≥ 200） |
| DW 的 U 型归一化分界点 | 通过实际数据分布校准，初始值 2/8 可调 |

---

## 八、实现路线图（供后续参考）

| 阶段 | 内容 | 依赖 |
|------|------|------|
| 阶段 1 | 实现 8 个指标的计算函数 | 复用 PathFinder/GridManager |
| 阶段 2 | 实现反向构造法关卡生成器 | 阶段 1 |
| 阶段 3 | 实现难度调控参数和策略 | 阶段 2 |
| 阶段 4 | 实现综合难度评分和等级划分 | 阶段 1 |
| 阶段 5 | 实现生成后验证和调整 | 阶段 1-3 |
| 阶段 6 | 验证校准（人工测试 + 参数调优） | 阶段 1-5 |
| 阶段 7 | 集成到游戏（难度选择界面） | 阶段 1-6 |
